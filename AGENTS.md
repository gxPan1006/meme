# 工程规范

## 通用\
- 不要加注释，除非我明确要求
- 以清晰、显式为先，避免过度技巧化。
- 倾向小模块、可组合与单一职责函数。

## 结构
- 可复用逻辑放 `src/`，`scripts/` 用于脚本。
- 避免循环依赖，保持单向依赖关系。
- 文件与模块命名反映主要职责。

## 错误处理与日志
- 对非法输入尽早失败并给出可执行提示。
- 外部调用失败需带上下文（接口、状态）。
- 面向机器的脚本输出结构化 JSON。

## 配置
- 密钥从环境变量读取，禁止硬编码。
- 可配置项提供 CLI 参数。
- 提供适合本地开发的默认值。
